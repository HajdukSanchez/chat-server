const CONNECT = 'connect' // Event to connect to the server
const DISCONNECT = 'disconnect' // Event to disconnect from the server
const CHAT_MESSAGE = 'chatMessage' // Event to send or recive a chat message

module.exports = {
  CONNECT,
  DISCONNECT,
  CHAT_MESSAGE,
}
